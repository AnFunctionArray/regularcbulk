typedef int a, b;

extern int printf(const char *, ...);

typedef int a;

int main() {
    unsigned long long int a;
    return printf("%d %x %p", (0, a = 0 ? (9 || 2 + 7) * 7 : (4 && 8) - 8 ? 7 : 6, (int)0 || printf("%s\n", "5")) + (5 * 6 + 7), 0, subunkn);
}